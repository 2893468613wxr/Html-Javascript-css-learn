类型转换：

JavaScript 类型转换是指将一种数据类型转换为另一种数据类型。

JavaScript 类型转换的规则：

1. 字符串转换为数字：parseInt() 和 parseFloat() 方法可以将字符串转换为数字。
2. 数字转换为字符串：Number() 方法可以将数字转换为字符串。
3. 布尔值转换为数字：true 转换为 1，false 转换为 0。
4. 数字转换为布尔值：任何非零和非空的数字都转换为 true，0 和 NaN 都转换为 false。   
5. 字符串转换为布尔值：任何非空字符串都转换为 true，空字符串（""）转换为 false。
6. 数组转换为字符串：join() 方法可以将数组转换为字符串。     
7. 字符串转换为数组：split() 方法可以将字符串转换为数组。     
8. 日期对象转换为字符串：toISOString() 方法可以将日期对象转换为 ISO 格式的字符串。     
9. 字符串转换为日期对象：Date() 方法可以将 ISO 格式的字符串转换为日期对象。     
10. 任意类型转换为字符串：String() 方法可以将任意类型转换为字符串。     
11. 任意类型转换为布尔值：Boolean() 方法可以将任意类型转换为布尔值。     
12. 任意类型转换为数字：Number() 方法可以将任意类型转换为数字。

2种类型转换：

- 显式转换：使用类型转换函数将一种数据类型转换为另一种数据类型。
- 隐式转换：某些运算符或函数会自动进行类型转换。

